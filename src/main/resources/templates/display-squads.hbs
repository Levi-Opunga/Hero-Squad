{{#partial "content"}}

    {{#if heroes}}
        <h2 class="display-2">Available Heroes</h2></div>
        {{#each squads}}
            <h2>Squad Name :{{name}}</h2>
            <h3>Motto</h3>
            <p>{{motto}}</p>
            <h3>Cause</h3>
            <p>Squad :{{Cause}}</p>

            <h4>Squad Capacity :{{max-size}}</h4>
        {{/each}} <a href="/hero-form">
            <button class="btn btn-success">Add More Heroes</button>
        </a>
    {{/if}}


{{/partial}}partial}}
    {{>layout.hbs}}